#include <stdio.h>
#include <string.h>

char* prefix = "Proxy: ";

int main() {
    method0(prefix);
    method1(prefix);
    method2(prefix);
    method3(prefix);
    method4(prefix);
    method5(prefix);
    method6(prefix);
    method7(prefix);
    method8(prefix);
    method9(prefix);
    return 0;
}